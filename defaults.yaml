# forge-text default configuration.
# Override: create config.yaml (gitignored) with only the fields you want to change.
#
# All skills in skills/ deploy to all providers. Provider targets and models
# come from the forge CLI's embedded defaults.
